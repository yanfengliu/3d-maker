# Devlog summary

Newest first. One short dated line per behaviour-changing session; see `detailed/` for anything a later session could trip over.

- 2026-09-17 — iPhone 17 Pro round 12 (loop round six: the last three recorded leftovers, one of them a gate). **The LiDAR window's reflectivity became per finish.** Round 9 left Deep Blue's window at 0.337 of its plateau against a 0.13–0.25 band, blaming an untinted dielectric for not holding one band across finishes. Re-measured on the `back` preset's own window rect (x 836 y 205 24×24) against the plateau's flat face (x 700 y 90 90×90), that span is **83–208 luma today** — the recorded 89 was 6 luma high, which is why 0.337 measures 0.361 here — and the window renders 30/30/33, so orange 0.201 and silver 0.159 are inside the band and only blue is not. What closes it is not a darker value but an **empty intersection**: the window must read ≤ 20.8 luma for blue's ceiling and ≥ 27.0 for silver's floor, and an untinted dielectric renders one luma whatever the frame is. `DARK_GLASS_REFLECT` in `palette.ts` therefore scales the Fresnel return per finish (deep-blue 0.378 of the base 0.45, the others 1) while the tint stays `0x090c12` on all three — the albedo lever was measured and rejected, rendering the window at **180 luma, a light-grey disc**. Blue landed 0.361 → 0.169 with orange and silver unmoved (reference close-up 0.127 through the same arithmetic), pinned in `optics.test.ts` with the arithmetic as an assertion and red-proved by setting deep-blue back to 1. **The button seam became a base shadow rather than a uniform band**: the reference's own crevice measures **0.76 mm** at 3.8 px/mm (from the two 11 mm volume pills), so `SEAM_BASE = 0.65` grows the seam only at the pill's −Y end (base band 0.755 mm) while the silhouette keeps its 0.105–0.167 mm hairline; `seamBand`/`expectSeamBand` were re-expressed as base + silhouette with both arms red-proved (a uniform 0.3 still fails the silhouette bound, a uniform 0.02 still fails the base bound). **The look gate gained a glass section** — front/right/left presets, one rect each, BT.709 percentiles: median band 45..85 on all three and spread ≥ 70 on front and right only, because `left` is a flat mirror whose reflected ray sweeps 0.1° and cannot carry structure (measured in round 11, stated in the gate's header so nobody "fixes" it). Both failure arms were produced on the live model over CDP: a slab arm (medians 111.1) and a void arm (medians 34.8/0.0/0.0 with spreads *rising*), each exit 1 — which is why the median is a band and not the ceiling-only bound the brief asked for. 55 tests in 9 files, typecheck, lint, build, and the gate's four sections (finishes, port, glass, graze) all green; poster re-shot.

 pupil crescent (42) pixels are byte-identical between arms, 55 tests in 9 files, typecheck, lint and build green. Recorded for the next session: `materials.ts` is at exactly 500 lines after compressing four neighbouring comments without dropping a fact, `optics.test.ts` is 555 and over the 500-line convention on a file this slice owned, the zoomed close-up preset sits just under the band on all three finishes (0.123/0.109/0.090) because that preset's plateau renders 146/64/199 against the back view's 154/83/208, and both review lanes were probed and are blocked (codex needs a newer CLI, claude's OAuth is expired) so no independent review of this slice happened. Also recorded: a temporary `window.location.search` ladder probe briefly sat in `materials.ts` and put the suite red for two sibling slices under vitest's node environment; it was removed within the same session and `materials.ts` was restored byte-for-byte before any other edit, and `src/` now carries no runtime switch of any kind.

- 2026-09-17 — iPhone 17 Pro round 11 (loop round five: button seams and measurement integrity). **The button pills read as decals**, and the measured cause was the seam's extent, not its finish: it stood 0.255–0.378 mm outside each pill's silhouette — a 1–2 px near-black ring around the whole pill — and had eaten the rail's flat metal down to 0.042 mm beside it. `SEAM_MARGIN` 0.3 → 0.15 gives a 0.105–0.166 mm band and 0.19 mm of metal back; the worker also *declined* the brief's premise that the pill's finish was wrong, showing the pill face and the rail's flat face render the identical rgb(184,82,50) and that the "glossy rail" is its 0.36 mm chamfer. The seam's tone then went from one fixed near-black to the finish's own albedo at `SEAM_SHADE = 0.2594` (per finish, because a fixed grey fitted on orange rendered 0.89 of Deep Blue's own pill face), taking the seam from 0.09–0.31 of the metal to 0.33–0.56 against a reference crevice at 0.494–0.572, and fixing the lens rings' base hairline which shares the material (0.146 → 0.500 against a reference 0.55–0.60). **The round also earned a measurement-integrity guard**: a worker patched the running app through a repo-root `.env` read by `import.meta.env` (every later capture on that dev server rendered the patched map), and separately found the Vite dev server serving **stale revisions** — four consecutive edits, four byte-identical measurements, a whole ladder invalid with no error. The sweep harness now fingerprints the served bytes of every module twice per sweep, exits 2 on a mismatch, and prints `served <digest>` plus a per-module `revision` line; it reproduced a real stale serve and its own bound is stated in its header. `AGENTS.md` now carries both rules, and `.env`/`.env.*` are gitignored. One item closed as physically expected rather than chased: the left profile's flat dark glass, whose flat mirror sweeps 0.1° of the room across its width, and whose two candidate fixes flattened the right profile and the front too. 54 tests in 9 files, typecheck, lint, build and the look gate all green.

- 2026-09-17 — iPhone 17 Pro round 10 (loop round four: the profiles and the port's bound). **The cover glass in profile was a uniform light-grey plane** — the most user-visible defect left — and is now dark glass with a bright reflection band (right profile median 118.1 → 58.4, spread 4.9 → 141.7; left median 140.9 → 53.4; the front preset's round-7 read preserved at spread 141.1). The mechanism was measured, not assumed: a clearcoat's Fresnel is *not* scaled by `specularIntensity` in three 0.185 (that lever moved the face 21 %), so the fix is three panels in `scene.ts`, one of which must be painted *after* the wall it dims — a load-bearing ordering now pinned as an assertion. Not reached: `left` is dark but still a flat field (spread 1.9) because its glass averages about a quarter of the environment map. **The look gate was extended to the port** (pocket ≤ 25, tongue 25–70, rail 70–200 plus ordering, always on), proved able to fail by mutating the live model over CDP since the model files belonged to another slice — and it caught a regression the glass work introduced (one roll pixel at luma 3, veneer floor raised to 0.075). **The mouth plate's burial claim became true and gated**: it had been sitting 0.02 mm below the rail — the model's lowest point — while the comments claimed it was buried in the frame's metal; it is now inset above the frame's face (0.019997 mm measured over 2114 samples), with a new gate against the built solid and the tongue's oblique gate strengthened from `> 0` to a 10 % floor at the documented camera direction. `materials.test.ts` was split (553 → 217) with the optics pins in a new `optics.test.ts`, restoring the 500-line rule with the test count unchanged at 53. Review status: both canonical lanes probed and still blocked, so the port rewrite went to a same-family adversarial lane — weaker than independent review, and reported as such; it reproduced the headline claim itself (823/823 blocked before, 0/823 after) and found the burial and tongue-gate problems. 53 tests in 9 files, typecheck, lint, build and the extended look gate all green.

- 2026-09-17 — iPhone 17 Pro round 9 (loop round three: audit the ungated surfaces). **The frame's USB-C cut never existed.** `slabGeometry` pushed the slot into `shape.holes`, and a hole whose outline crosses the shape's outline is undefined for earcut — so the frame was solid at the port and the entire read came from the bottom assembly: a dark liner hanging 0.01 mm below the rail (the hard dark frame) and the tongue's bright underside (the grey trapezoid). `port.ts` now draws the notch into the frame's own contour, giving a real 8.4 × 3.2 opening with the documented 1.1 mm corners (measured deviation from the ideal curve 0.0000 mm), with a mouth plate, a dark face, one cavity plate inside the pocket, and a small tongue low in the mouth's deep half; the tongue's tone went `0xa9aeb6` light steel to `0x343841` dark steel (rendered 111 → 43 luma against the pocket's 11 and the rail's 95). **The dark optics over-responded to the working environment** and were brought to reference: the LiDAR window 43 → 18 luma (0.122 of its plateau against a reference 0.127), the lens-glass rim liner 169 → 102 (reference 88–119) and the pupil crescent p95 169 → 92, with the lever identified by experiment as three's `specularF90` rather than roughness. **Two surfaces that had no gate got one** — the Apple logo's placement/size and the MagSafe ring — and the MagSafe gate records that the ring's invisibility rests on a tessellation accident (a 10-segment torus never samples its equator, so its effective tube is 0.713 mm not 0.75) rather than on its intended stand-off. Reported and rejected on measurement: my claim that the logo should read 0.62 of its panel — the Apple hero photo measures 0.767, the close-up 0.76, and the render's 0.769 already matches. 51 tests in 7 files, typecheck, lint, build, and the look gate all green.

- 2026-09-17 — iPhone 17 Pro round 8 (loop round two): the look got a gate and an entire feature was found missing. `scripts/check-finishes.mjs` (`npm run check:finishes`) drives its own headless Chrome, reads the `back` view's canvas back for all three finishes and fails when a rail-over-panel luminance ratio leaves ±0.05 of its reference (orange 0.73, blue 0.47, silver 0.87 — measured 0.747/0.508/0.866), with `--graze` scanning a roll close-up for any run of luma ≤ 3 through the page's real pointer input; exit 2 means it could not measure, which is not the same claim as a failed look. `src/iphone/materials.test.ts` pins the look's inputs so CI (no Chrome) still catches drift — two halves of one gate, documented as such in `AGENTS.md`. Fifteen inert `envMapIntensity` values were deleted after confirming nothing reads them (the renderer overwrites them with `scene.environmentIntensity`), with an 11-frame A/B showing 0 of 1,400,000 pixels differ and the served module checked so the A/B could not pass vacuously. The audit found the US variant's **5G mmWave antenna window** missing from the top edge — a 28.76 mm stadium insert, 0.40 of the body width re-derived from the reference photo's own pixels, now built with four gates red-proved five ways, and given its own `mmwave` material (dielectric, roughness 0.82, no clearcoat) that reads 0.833/0.831/0.836 of the frame against the reference's 0.81 and is flat across its face as the real insert is. The audit harness itself was repaired: its 700 ms settle left 93 px of run-to-run variance, so every earlier audit frame — including a round of "no black band" claims — was shot at a slightly non-reproducible pose; it now converges on stillness in frames and three runs are byte-identical, and it refuses a no-op input, a typo'd view, or a page reload mid-sweep. 40 tests in 6 files, typecheck, lint, build, and the look gate all green.

- 2026-09-17 — iPhone 17 Pro round 7 (continuous-improvement loop, first round): the studio environment **had never reached the render** — `scene.environment = null` and the PMREM produced byte-identical frames and `scene.environmentIntensity` moved no pixel, so every round-5/6 material conclusion (including the `metalness: 0.15` fudge's rationale) was measured against black. The PMREM path is replaced by a procedurally drawn equirect canvas, and a second three.js fact came with it: a standard material's own `envMapIntensity` is overwritten by `scene.environmentIntensity` (`WebGLRenderer.js:2694`), so every per-material value in `materials.ts` has never been read. With a working environment the frame went to `metalness: 0.70` and the rail/panel luminance ratios landed inside ±0.05 for the first time (orange 0.747 vs 0.73, blue 0.508 vs 0.47 — the round-6 0.09 miss closed — silver 0.866 vs 0.87), the graze return of the black band stayed at 0 px of luma ≤ 3, and the back panel gained a real gradient. Three looks the new environment then broke were fixed in a corrective slice, each after naming its mechanism: the front was a uniform grey slab because a flat mirror 1702 mm from a wall-sized source sweeps only 2.4° and returns one value (now two small cards inside that cone — mean 64, p02 44, max 110); the ring glints were Fresnel at grazing incidence, not the environment (ring roughness 0.42, 0 px ≥ 235, was 41/63/59 px); and silver's frame at roughness 0.92 fell out of band (0.670) and is back at 0.42/0.866 with a per-finish panel lift. MagSafe was proved to draw no pixel at all by painting it pure red and diffing — it is invisible because it is buried, not because of its roughness step. New tool `scripts/review-lanes.mjs` probes the codex/claude reviewer lanes with evidence (both currently blocked: codex's pinned model needs a newer CLI, claude's OAuth session expired) and `AGENTS.md` now requires that probe before any claim that independent review is unavailable. 32 tests in 5 files, typecheck, lint, build green; poster re-shot.

- 2026-09-17 — iPhone 17 Pro round 6 (structural realism, after the user reported three defects five rounds of preset-view inspection had shipped past): the Apple logo was mirrored on the back (it read correct from +Z, but the panel faces −Z; `logo.ts` now mirrors X too, and `SVGLoader` — which needs a DOM and was why the class had no node gate — is replaced by a self-contained path reader), the camera plateau was a machined flat-wall step where the real bump is a forged roll that wraps over the phone's top (`src/iphone/plateau.ts` lofts the roll; the first build swept it around the whole rail outline, making the bump a full-back plate, which passed 22 green tests and rendered black gashes), and the USB-C port pierced the whole body (`src/iphone/port.ts` now closes both faces with aluminum plates and leaves a downward-only pocket; its mouth had also been clipped to a 6.7 × 1.0 mm lens). The audit also found the bottom had 6+4 bores (real: 5+5), the panel top corners did not wrap the bump, the ring crowns were fully sloped, and Camera Control read raised. Materials: `anisotropy` 0.55 → 0 (a hard black band along the roll was measured as that term — it survived roughness, environment and normal-map changes, so my own diagnosis in the brief was wrong), then `metalness` 0.9 → 0.15 with `envMapIntensity` 2.6 to restore the brightness that cost. That pair was picked against reference photos' own rail-over-panel relative luminance: orange 0.07x → 0.69x (gsmr-040 0.73x), silver 0.15x → 0.91x (lineup 0.87x), blue 0.03x → 0.38x (gsmr-019 0.47x) — orange and silver within 0.05 of the reference, blue 19 % under its own. The two comparison arms behind it moved `metalness` and `envMapIntensity` together, so they identify no lever. New tool `scripts/sweep-iphone.mjs` orbits/zooms through the real controls over CDP for the feature-by-feature audit `AGENTS.md` now requires. 29 tests (23 parts + 6 models), typecheck, lint, build green; poster re-shot.

- 2026-09-16 — iPhone 17 Pro round 5 (finish fidelity vs Apple's official photos): lens rings are now finish-tinted polished metal (was neutral chrome), the lens glass grew to 0.77 of the ring with its envMapIntensity lifted off 0.28 (the "black void" cause), the flash is a matte pale window with a hairline collar, LiDAR/mic got thin dark surrounds, and the camera-cluster X positions were retuned against Apple's lineup photo (telephoto now reaches across the plateau's centre line). Panel/logo/MagSafe/antenna/sapphire tones are all solved per-finish in the new `src/iphone/palette.ts` (materials.ts re-exports the public surface unchanged; HSL maths must name `THREE.SRGBColorSpace` because `THREE.Color` stores linear — a first-pass panel came out *darker* than its frame). MagSafe is now panel-colour exactly with only a roughness step, invisible straight-on; silver's rings carry roughness 0.35 so they read bright instead of mirroring the dark studio; `applyColorway` repaints ring colour *and* roughness (a dropdown switch used to leave an orange ring on a blue phone). Poster re-shot. 18 tests, typecheck, lint, build green; independent read-only review by kimi-k3.

- 2026-09-16 — Repo re-purposed to a 3D model showcase (owner directive): the evolution-studio/voxel direction is abandoned, so `voxel` left `package.json` and `package-lock.json` (zero voxel entries left; the stale `../voxel` extraneous lock entry needed a manual delete, `npm install` kept re-adding it), `src/core/` (genome/build/edit/sweep plus both test files) was deleted with it, and `scripts/peek-session.mjs` went too. `docs/design/spec.md` is rewritten as the showcase spec and `model-studio.md` deleted; `AGENTS.md` repo sections rewritten for the showcase with the FLEET-CANON block byte-identical. Pages CI builds through the full gate again (`npm run build`, not `npx vite build`). The viewer gained an additive `?noui=1` (shot mode only) so the index poster no longer bakes the overlay panel in: re-shot at 1400×1000 through `scripts/capture-shots.mjs`, and a `?noui`-less re-run is byte-identical to the old hero shot. `npm audit fix` closed 5 pre-existing advisories (vitest 4.1.11, brace-expansion, nanoid, postcss) so both audit gates read `found 0 vulnerabilities`. 18 tests (12 iphone + 6 index), typecheck, lint (zero warnings), build green. Not committed.

- 2026-09-16 — Index page at the repo root (`index.html`, `src/index/`): a card per entry in the new `src/index/models.ts` registry, so the Pages root resolves instead of 404ing. The iPhone poster was promoted from the shot matrix into `public/posters/`, and `src/index/models.test.ts` checks each entry's fields, unique ids, `./`-relative hrefs and that the poster file really exists under `public/`. `scripts/capture-shots.mjs` gained `SHOT_PAGE` (default unchanged), and `vite.config.ts` now builds both pages. Typecheck, lint, 38 tests (32 existing + 6 new), build green. Not committed.

- 2026-09-16 — iPhone 17 Pro round 4: `RAIL` was 0.36 mm wide of the frame's *measured* wall (the chamfer counted twice), so every edge part floated; the button seam shared the pill's outer plane, which is what made the pills black and speckled; back-facing slabs were built from their outer plane, which detached the plateau and buried the lens stack 2.095 mm; the lens dome's skirt folded through its own cap (the 6 o'clock notch); the flash's emissive was the blown-out disc. New gate `src/iphone/parts.test.ts` (12 tests) pins `RAIL` to the frame's own `Box3` and every edge part's outermost point to the rail plus its documented stand-off — it caught a 0.67 mm mic window in a 0.75 mm hole on its first run, and each bound is proved red by reintroducing its defect. Typecheck, lint, 32 tests (20 existing + 12 new), build green. Not committed.

- 2026-09-16 — iPhone 17 Pro round 3: every edge defect was a part placed off the wrong surface. The frame's real side wall is one bevel outside `BODY.width / 2` (now `RAIL`), so the ports, bands and buttons move onto it; the speaker/mic/top-mic tubes became flush bore mouths; the Dynamic Island lost a leaning-plane correction it never needed; the lens stack's barrel floor moved behind the glass; the studio's two side softboxes stopped facing away from the room. Typecheck, lint, 20 existing tests, build green. Not committed.
- 2026-09-16 — Added the iPhone 17 Pro viewer page (`iphone.html`, `src/iphone/`): procedural 1:1 mm model, three live colourways, eight `?view=` presets behind the `?shot=1` contract. Typecheck, lint, 20 existing tests, and build all green. Not committed.
